import randOne from "./catArray";

// Reload cat image

const reload = () => {
    document.getElementsByClassName('vote');
    for(var i = 0; i < refresh.length; i++) {
        addEventListener('click', randOne);
};